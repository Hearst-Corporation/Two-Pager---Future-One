# dependencies
node_modules/

# Next.js
.next/
out/

# TypeScript incremental build cache
*.tsbuildinfo

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
.vercel

# Brochure print exports (generated)
export/
futur-one-brochure.zip

# Large media — keep on disk, never version
Google Chrome.mp4

# Raw Midjourney source assets (use renamed versions instead)
public/u2883995211_*

# QA & audit artefacts (kept local, not versioned)
audit-screenshots/
.playwright-mcp/
docs/audit/*.html
cockpit-*.png
cockpit-step*.png
hearst-*.png
advisor-*.png
admin-dashboard-*.png
prese-hub-*.png
m5-*.png
dashboard-*.png
smoketest-*.png
test-*.png
qa-*.png
qa-fix-*.png
qa-validate-*.png
memo-*.png
*.tgz
.env.local.bak

# Agent worktrees (Claude Code orchestrator)
.claude/

# Test outputs (vitest + playwright)
coverage/
playwright-report/
test-results/

# Large video assets — must be hosted externally (Supabase Storage / Vercel Blob)
# See SectionHub.jsx for the src= URL that must be updated.
public/FUTUR-ONE-FINAL.mp4
public/FUTUR-ONE-FINAL.backup.mp4

# Runway / orchestrator state artefacts (local only)
runway-state.json
runway-state-final.json
runway-tasks.log

# debug screenshots
simulator-*.png
A[0-9]-*.png
*-simulator*.png

# audit / QA artefacts à la racine (générés, non versionnés)
/coherence-final-*.png
/batch*-*.png
/declutter-*.png
/rail-nav-*.png
/oracle-cta-element.png
/oracle-sim-raw.png
/uiux-audit-*.html
*.bak
/brief-*.png
/hardware-*.png
/opmodel-*.png
/section-*.png
/other-sections.png
/financial-*.png

# screenshots de session (smoke / verify / scan) — générés par Playwright/agents, jamais versionnés
/smoke-*.png
/screenshot_*.png
/screenshot-*.png
/val-*.png
/verify-*.png
/scenario*.png
/campus3d-*.png
/dossier-*.png
/executive-*.png
/recovery-*.png
/deals-*.png
/_concept-*.png
/futurone-*.png
/tmp-*.png

# Business-plan / investment-brief deliverables (binaries — kept local, hosted externally)
/Futur-One-*.pdf
/models/

# Capability / hardcode audit outputs (kept local, not versioned)
/docs/*-audit.md
/docs/*-audit.json
