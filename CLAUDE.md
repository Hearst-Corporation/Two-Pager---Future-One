# CLAUDE.md — Oracle

Cockpit investissement infrastructure (Qatar / Hearst). Parcours : simulateur → résultats → mémo → dossier → PDF.

Stack : Next.js 14 · React 18 · Supabase (`crm`) · OpenAI · `@hearst/cockpit-shell` (copie locale dans `cockpit-shell/`).

Dev : `npm run dev` → port **5005**. Gate : `npm run check`.

Pages : `app/(cockpit)/admin/hearst/` (simulator, financial, deals, workspace, dossier, sources).
