# calculations/ — RÉSERVÉ (vide)

Fonctions PURES et déterministes. Pas de réseau, pas de LLM, pas d'aléatoire.

Chaîne attendue : gross revenue → EBITDA → consortium cash flow → terminal value →
MOIC → IRR. Chaque fonction arrive avec son test qui pin les chiffres du HTML canonique.

Ne PAS importer l'ancien moteur (`lib/hearst-calculations.js`).

`TO_BE_REPLACED_BY_CANONICAL_QATAR_MODEL`
