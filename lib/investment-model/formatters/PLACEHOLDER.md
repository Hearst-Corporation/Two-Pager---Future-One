# formatters/ — RÉSERVÉ (vide)

Mise en forme d'affichage uniquement (USD, %, x, MW, années).

PEUT réutiliser `lib/hearst-format.js` (générique, KEEP) plutôt que de redéfinir.

`TO_BE_REPLACED_BY_CANONICAL_QATAR_MODEL`
