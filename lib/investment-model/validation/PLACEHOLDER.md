# validation/ — RÉSERVÉ (vide)

Garde-fous du modèle : pas de nombre inventé. Champ manquant = `null` + label
explicite (réutiliser la convention `MISSING_LABEL` = 'N/A — Source Required' du repo).

Valide que chaque output est traçable à une assumption, elle-même traçable au HTML canonique.

`TO_BE_REPLACED_BY_CANONICAL_QATAR_MODEL`
